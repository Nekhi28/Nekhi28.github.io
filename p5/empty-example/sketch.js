var starX = 200;
var starY = 100;

function setup() {
  createCanvas(displayWidth,displayHeight);
  background('rgba(0, 255, 0, 0.25)');
}

function draw() {
  // put drawing code here
  ellipse(starX,starY,starY,starY);
}
